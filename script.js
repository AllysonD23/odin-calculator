const add = function(a, b) {
	return a + b;
};

const subtract = function(a, b) {
	return a - b;
};

const multiply = function(array) {
    return a * b;
  };

const divide = function(a, b) {
    return a / b;
}

let firstNumber;

let operator;

let secondNumber;

const operatorFunction = function(operator, firstNumber, secondNumber) {
    return result = operator(firstNumber, secondNumber);
}